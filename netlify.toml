[build]
  command = "npm install"
  functions = "functions" 
[dev]
  command = "npm rebuild"
  publish = "dist"
[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "/api/*"
  status = 200
  to = "/.netlify/functions/api/:splat"
